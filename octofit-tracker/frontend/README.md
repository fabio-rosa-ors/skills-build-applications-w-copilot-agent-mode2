# OctoFit Tracker Frontend (scaffold)

This folder contains a minimal React + Bootstrap scaffold.

Install dependencies and run using npm with `--prefix` to ensure the install targets this directory from workspace root:

```bash
# from workspace root
npm install --prefix octofit-tracker/frontend
npm run --prefix octofit-tracker/frontend start
```

The scaffold imports Bootstrap CSS in `src/index.js`.
